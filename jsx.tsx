//rename file to index.tsx
//json config file uncomment "jsx": Preserve => rename to  "jsx": "react",
//npm i react
// npm i -D @types/react

import React from "react";

const a: JSX.Element = (
  <div tabIndex={0}>
    {1 + 1}
    <a>Hello</a>
  </div>
);
